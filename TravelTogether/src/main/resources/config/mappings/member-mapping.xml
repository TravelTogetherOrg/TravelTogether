<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberDAO">
			<!-- vo                db table -->
	<resultMap type="member" id="memberResult">
		<id property="member_id"  column="MEMBER_ID"/>
		<result property="member_password" column="MEMBER_PASSWORD"/>
		<result property="member_name" column="MEMBER_NAME"/>
		<result property="member_nickname" column="MEMBER_NICKNAME"/>
		<result property="member_gender" column="MEMBER_GENDER"/>
		<result property="member_birthday" column="MEMBER_BIRTHDAY"/>
		<result property="member_create_date" column="MEMBER_CREATE_DATE"/>
		<result property="member_profile_image" column="MEMBER_PROFILE_IMAGE"/>
	</resultMap>
	
	<insert id="insertMember" parameterType="member">
		INSERT INTO MEMBER(MEMBER_ID, MEMBER_PASSWORD, MEMBER_NAME, MEMBER_NICKNAME, MEMBER_GENDER, MEMBER_BIRTHDAY, MEMBER_CREATE_DATE, MEMBER_PROFILE_IMAGE)
			VALUES(#{member_id}, #{member_password}, #{member_name}, #{member_nickname}, #{member_gender}, #{member_birthday}, now(), null);
	</insert>
	
	<update id="updateMember">
		UPDATE MEMBER SET MEMBER_PASSWORD=#{member_password}, MEMBER_NAME=#{member_name}, MEMBER_NICKNAME=#{member_nickname}, MEMBER_PROFILE_IMAGE=#{member_profile_image} 
			WHERE MEMBER_ID=#{member_id}
	</update>
	
	<delete id="deleteMember">
		DELETE FROM MEMBER WHERE MEMBER_ID=#{member_id}
	</delete>
	
	<!-- 전체검색 -->
	<select id="getMemberList" resultMap="memberResult">
		SELECT * FROM MEMBER
	</select>
	
	<!-- 상세검색 -->
	<select id="getMember" resultType="member" >		
		SELECT * FROM MEMBER WHERE MEMBER_ID = #{member_id}		
	</select>
	
</mapper>